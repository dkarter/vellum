[package]
name = "vellum"
version = "1.0.0"
edition = "2024"
description = "A fast, customizable terminal multiplexer menu"
license = "MIT"
repository = "https://github.com/dorian/vellum"
rust-version = "1.88"

[[bin]]
name = "vlm"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
crossterm = "0.29"
json_comments = "0.2.2"
noyalib = "0.0.28"
nucleo-matcher = "0.3"
ratatui = "0.30"
rusqlite = { version = "0.40", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "1.0"
unicode-segmentation = "1.12"

[dev-dependencies]
pretty_assertions = "1.4"

[profile.release]
lto = "thin"
strip = true

[target."cfg(unix)".dependencies]
nix = { version = "0.29", default-features = false, features = ["signal", "process", "fs"] }
